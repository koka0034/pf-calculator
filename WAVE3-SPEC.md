# Волна 3 — роли, изоляция сессий, чистка UI

Ветка: `wave3-roles-sessions`. Клиент-only (Supabase SQL не трогаем).

## 1. Изоляция по коду сессии
- Ключ хранения на сервере = **код сессии** (было `ресторан::склад`).
- Ресторан (выпадашка 60 шт) — удалить совсем.
- Дата как поле — удалить. Отчёт/сверка → авто «сегодня». Заполненный бланк → имя = `исходное_имя + "_заполненный"`.
- Нет сессии → выгрузка на сервер недоступна (локальный режим работает).

## 2. Роли (авто + ручная ссылка)
- Создал сессию → **админ**. Вошёл по коду → **работник**. Один → админ.
- Ссылка «я руководитель» — ручное переключение. Роль хранится локально.

## 3. Вкладки и видимость
### 👤 Работник (2 вкладки, сверху бейдж «Сессия ABC123 · Бар»)
- **Товары** — подсчёт (склад/список пришли по коду, сменить НЕЛЬЗЯ) + сброс своей страницы («Очистить только товары»)
- **ПФ** — ввод остатков настоек, БЕЗ сверки/± + сброс своей страницы («Очистить только ПФ»)
- Внизу: **«Выгрузить мой подсчёт»** + **«Загрузить последний выгруз»**
- Скрыто: создание сессии, приём, расхождения-для-печати, импорт, печать, сброс всего

### 👨‍💼 Админ (3 вкладки)
- **Управление** — создать сессию (склад→код), принять с сервера, расхождения (для печати отчёта), импорт бланка / остатки из отчёта / заполнить бланк, печать, **«Очистить весь подсчёт»**
- **Товары** — подсчёт + режим сверки (из ПФ / ±) + сброс страницы + расхождения ± (руководитель считает и сразу видит +/−)
- **ПФ** — ввод + разборка + сброс страницы

## 4. Кнопки — точная семантика
- Сброс на **Товары** = очистить только товары (эта страница)
- Сброс на **ПФ** = очистить только ПФ (эта страница)
- Сброс на **Управлении** = очистить ВЕСЬ подсчёт (товары+ПФ)
- Расхождения в 2 местах — оба нужны (Товары=сверка счётчику, Управление=печать)
- Работник: «Выгрузить мой подсчёт» (было «Сохранить-выгрузить-очистить»)
- Работник: «Загрузить последний выгруз» — восстанавливает в поля снимок последней успешной выгрузки (`last_upload`, сохраняется локально при каждом upsert)

## 5. Инструкции
Переписать под: роли, вход по коду, изоляция по сессии, новые кнопки, отчёт=сегодня, бланк=имя_заполненный. Убрать ресторан/дату.

## 6. Сохранить (не сломать)
- Логику безопасности Волны 1: guard пустого снимка, приём с подтверждением, busy-lock, iOS «Дальше», очистка только выгруженного.
- Барный редизайн Волны 2.
- Историю локальных подсчётов — оставить у админа.
